/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['picsum.photos', 'flagcdn.com', 'flagcdn.com', 'upload.wikimedia.org'],

  },
  
}
export default nextConfig;
// import type { NextConfig } from "next";

// const nextConfig: NextConfig = {
//   /* config options here */
//   images: {
//     domains: ['picsum.photos', 'flagcdn.com', 'flagcdn.com', 'upload.wikimedia.org'],

//   },
// };

// export default nextConfig;

